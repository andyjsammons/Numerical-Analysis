%this script solves Ax = b in three different ways:
% 1) GEPP (Gauss Elimination using partial pivoting
% 2) using the inverse
% 3) using Cramer's rule
% it will then time each method.

function b = SolveXCompare(A,b)






